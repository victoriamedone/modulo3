
namespace misFunciones {
    export let areaOfCircle=(radius: number): number =>{
        return (Math.PI * (Math.pow(radius,2)));
    }

    export let areaOfSquare = (side: number): number =>{
        return side + side
    }
}

 export{misFunciones}